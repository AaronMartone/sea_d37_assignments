(function() {

    'use strict';

    // Dictionaries are 'hashes', 'associative arrays', 'maps' and 'symbol tables'.
    function inherits(obj, prop) {
        return !!(obj[prop] && !obj.hasOwnProperty(prop));
    }

})();